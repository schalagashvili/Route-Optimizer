import React from 'react'
import { MainWrapper, Overlay, Button } from './styles'

const DetailsPopup = () => (
  <MainWrapper>
    <Overlay>
      <Button>Close</Button>
    </Overlay>
  </MainWrapper>
)

export default DetailsPopup
